services:
  web:
    build: ./sam-dashboard
    ports:
      - "7070:80"
    depends_on:
      - backend

  backend:
    build: ./chiller-backend
    ports:
      - "3001:3001"
    env_file:
      - ./chiller-backend/.env
    restart: unless-stopped
